# Data Description and Policy

## Dataset Overview
У проєкті використовується датасет **NYC Yellow Taxi Trip Data**, який містить інформацію про поїздки жовтих таксі в місті Нью-Йорк. Дані включають часові, просторові та фінансові характеристики кожної поїздки та широко застосовуються для аналізу транспортних патернів і міської мобільності.

## Data Source
Датасет отримано з платформи **:contentReference[oaicite:0]{index=0}**:

🔗 https://www.kaggle.com/datasets/elemento/nyc-yellow-taxi-trip-data

Даний набір даних є підготовленою версією відкритих даних, первинно опублікованих Taxi & Limousine Commission (TLC) міста Нью-Йорк.

## Data Content
Датасет містить, зокрема, такі поля:
- `tpep_pickup_datetime` — час початку поїздки  
- `tpep_dropoff_datetime` — час завершення поїздки  
- `passenger_count` — кількість пасажирів  
- `trip_distance` — відстань поїздки  
- `PULocationID` — локація посадки  
- `DOLocationID` — локація висадки  
- `fare_amount` — базова вартість поїздки  
- `total_amount` — загальна сума оплати  
- `payment_type` — спосіб оплати  

Формат файлів — CSV.

## Data Volume
Датасет має великий обсяг і може складатися з кількох файлів, кожен з яких містить сотні тисяч або мільйони записів. Через це сирі дані **не зберігаються безпосередньо в репозиторії**.

## Data Download Instructions
Для завантаження датасету використовується Python-скрипт із застосуванням бібліотеки `kagglehub`.

### Інструкція:
1. Переконайтесь, що у вас налаштований доступ до Kaggle API (обліковий запис та токен).
2. Встановіть необхідну бібліотеку:
   ```bash
   pip install kagglehub
3. Запустіть скрипт завантаження даних:

    ```bash
    python data/load_data.py

Скрипт автоматично:
- створить директорію `data/raw/`,
- завантажить датасет з Kaggle,
- збереже всі файли у `data/raw/`.

## Data Policy

- Сирі файли датасету **виключені з Git-репозиторію** та додані до `.gitignore`.
- У репозиторії зберігається лише код для завантаження, обробки та аналізу даних.
- Такий підхід забезпечує **відтворюваність експериментів** без порушення політики зберігання великих файлів.

## License

Датасет поширюється відповідно до ліцензійних умов, визначених на сторінці Kaggle.  
Користувач несе відповідальність за дотримання умов використання даних під час подальшого аналізу та публікації результатів.


